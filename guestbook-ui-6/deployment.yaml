apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "guestbook-ui-6"
  namespace: "client-apps"
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: "guestbook-ui-6"
  template:
    metadata:
      labels:
        app: "guestbook-ui-6"
    spec:
      containers:
      - name: "guestbook-ui-6"
        image: "gcr.io/google-samples/gb-frontend:v5"
        ports:
        - containerPort: 80
